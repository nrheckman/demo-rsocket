plugins {
	id 'idea'
	id 'com.palantir.git-version' version '0.12.3'
}

group = 'xyz.heckman.demo'
version gitVersion()

allprojects {
	repositories {
		mavenLocal()
		mavenCentral()
	}
	version = rootProject.version
}
